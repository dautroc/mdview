Inline math $E = mc^2$ inside a sentence.

A fraction: $\frac{a}{b}$ and a comparison $a < b$.

Display math:

$$
\int_0^1 x^2 \, dx = \frac{1}{3}
$$

A dollar amount that is not math: costs $5 and $10.

Math inside code should stay literal: `$not math$`.
