A rust block:

```rust
fn main() {
    println!("hi <world> & friends");
}
```

An unlabelled block:

```
plain text < > &
```

A mermaid diagram:

```mermaid
graph TD;
  A[Start] --> B{Choice};
  B --> C[End];
```

An unterminated fence follows:

```python
print("no closing fence")
